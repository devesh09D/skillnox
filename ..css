/* Base Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    line-height: 1.6;
    background-color: #f9f9f9; /* Light background */
    color: #333;
}

/* Header */
header {
    background: linear-gradient(to right, #ff4d4d, #d11a2a); /* Shades of red */
    color: white;
    text-align: center;
    padding: 2rem 1rem;
}

header h1 {
    margin: 0;
    font-size: 3rem;
}

header p {
    font-size: 1.2rem;
    margin-top: 0.5rem;
}

/* Hero Section */
.hero {
    background: white;
    padding: 3rem 1rem;
    text-align: center;
    border-bottom: 5px solid #d11a2a; /* Red border for emphasis */
}

.hero h2 {
    margin-bottom: 1rem;
    font-size: 2.5rem;
    color: #333;
}

.hero p {
    font-size: 1.2rem;
    color: #555;
}

.hero-buttons a {
    margin: 0.5rem;
    padding: 0.8rem 1.5rem;
    border: 2px solid #d11a2a;
    color: #d11a2a;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    transition: all 0.3s ease-in-out;
}

.hero-buttons a:hover {
    background: #d11a2a;
    color: white;
}

/* About Section */
#about {
    padding: 2rem 1rem;
    background: #fefefe; /* Slightly off-white */
    text-align: center;
    border-top: 5px solid #ff4d4d;
}

#about h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: #d11a2a; /* Red for headings */
}

#about p {
    font-size: 1.1rem;
    color: #555;
}

/* Contact Section */
#contact {
    padding: 2rem;
    background: #fff;
    text-align: center;
    border-top: 5px solid #d11a2a;
}

#contact h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: #d11a2a;
}

.contact-links a {
    margin: 0.5rem;
    padding: 0.5rem 1rem;
    text-decoration: none;
    font-weight: bold;
    color: #333;
    background: #ff4d4d;
    border-radius: 5px;
    transition: all 0.3s ease-in-out;
}

.contact-links a:hover {
    background: #d11a2a;
    color: white;
}

/* Registration Section */
#registration {
    padding: 2rem 1rem;
    text-align: center;
}

#registration h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: #d11a2a;
}

#registration p {
    font-size: 1.1rem;
    color: #555;
}

.registration-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    margin-top: 2rem;
    justify-items: center;
}

.registration-list a {
    text-decoration: none;
    font-weight: bold;
    color: white;
    background: #d11a2a;
    padding: 1rem;
    border-radius: 10px; /* Rounded corners for smoothness */
    transition: all 0.3s ease-in-out;
    display: block;
    text-align: center;
    width: 100%; /* Full width of grid cell */
    max-width: 250px; /* Limit the width */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
}

.registration-list a:hover {
    background: #ff4d4d;
    color: black;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2); /* Larger shadow on hover */
    transform: translateY(-4px); /* Lift effect */
}

/* Footer */
footer {
    background: black;
    color: white;
    padding: 1rem;
    text-align: center;
}

footer a {
    color: #ff4d4d;
    text-decoration: none;
    font-weight: bold;
}

footer a:hover {
    text-decoration: underline;
}
